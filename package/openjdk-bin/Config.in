config BR2_PACKAGE_OPENJDK_BIN
	bool "openjdk (precompiled glibc binaries)"
	depends on BR2_x86_64
	depends on BR2_TOOLCHAIN_EXTERNAL_GLIBC
	help
	  Precompiled openjdk runtime based on the openjdk debian packages.
