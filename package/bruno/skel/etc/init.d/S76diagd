#!/bin/sh
case "$1" in
  start)
    mkdir -p /user/diag
    babysit 60 diagd 2>&1 | logos diagd &
    ;;
  stop)
    pkillwait -x diagd
    ;;
  restart)
    $0 start; $0 stop
    ;;
  *)
    echo "Usage: $0 {start|stop|restart}"
    exit 1
esac
