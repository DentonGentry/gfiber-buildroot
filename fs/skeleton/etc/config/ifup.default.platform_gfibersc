iface="$1"
ip link set dev "$iface" up
if [ "$iface" = br0 ]; then
  # FIXME(kedong): For now using static IP, but we need to bring up dhclient
  # on the final product.
  # run-dhclient "$iface"
  ip addr add 192.168.1.1/24 dev br0
fi
