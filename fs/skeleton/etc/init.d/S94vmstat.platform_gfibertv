#!/bin/sh
case "$1" in
  start)
    babysit 60 vmstat 15 2>&1 | logos vmstat &
    ;;
  stop)
    pkillwait -x vmstat
    ;;
  restart|reload)
    ;;
  *)
    echo "Usage: $0 {start|stop|restart}"
    exit 1
esac
