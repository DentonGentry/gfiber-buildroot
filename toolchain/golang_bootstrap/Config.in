config BR2_TOOLCHAIN_GOLANG_BOOTSTRAP
	bool "Build Go language (golang) bootstrap version."
	depends on BR2_arm || BR2_i386 || BR2_x86_64
	default n
	help
	  This is the Go language bootstrap version.

	  http://golang.org
